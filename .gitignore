# Separate repository (has its own git remote)
saa-sites/

# Astro / Node build artifacts
node_modules/
dist/
.astro/
.vercel/
.output/

# secrets
.env
.env.*
!.env.example

# Stray nested repo (not part of this site)
_demo-repo/
